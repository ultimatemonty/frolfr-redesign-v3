export default function (server) {
  let course = server.createList('course', 10);
  let player = server.create('user');
  // let round = server.create('round', { course });
  // let scorecard = server.create('scorecard', { round, player });
}
